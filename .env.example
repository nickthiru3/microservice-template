# Environment configuration
ENV_NAME=local
APP_BASE_PATH=/super-deals

# Service Configuration
SERVICE_NAME=your-service-name
SERVICE_DISPLAY_NAME=Your Service Display Name

# Repository Configuration (for CI/CD)
GITHUB_REPO=your-org/your-repo
GITHUB_BRANCH=release

# AWS Configuration
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_DEFAULT_REGION=us-east-1
AWS_ACCOUNT_ID=your-account-id

# CodeStar Connection for GitHub integration (required for non-local envs)
# Get this ID after creating a CodeStar connection in AWS Console
CODESTAR_CONNECTION_ID=your-codestar-connection-id

# Resource naming is derived from SERVICE_NAME.
# Parameter Store base path is derived from APP_BASE_PATH (defaults handled in code).
